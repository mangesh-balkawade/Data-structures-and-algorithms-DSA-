/**
 * @param {number[]} arr1
 * @param {number[]} arr2
 * @return {number[]}
 */
var nextGreaterElement = function (arr1, arr2) {
  let map = {
    [arr2[arr2.length - 1]]: -1,
  };
  let stack = [];

  for (let i = arr2.length; i < arr2.length; i++) {}
};
